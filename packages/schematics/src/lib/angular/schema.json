{
    "$schema": "http://json-schema.org/schema",
    "id": "AngularSchematicService",
    "title": "Angular Schematic Service",
    "type": "object",
    "properties": {
        "document": {
            "type": "string",
            "description": "object definition for build endpoints consumption",
            "visible": true
        },
        "path": {
            "type": "string",
            "format": "path",
            "description": "The path where should create the service.",
            "visible": true
        }
    },
    "required": [
        "document"
    ]
}